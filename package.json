{
  "name": "planner",
  "version": "1.0.0",
  "description": "Planner for Busy People",
  "scripts": {
	"install": "cd server && yarn install && cd ../client-web && yarn install",
    "build": "cd client-web && yarn build",
    "start": "concurrently --kill-others-on-fail \"cd server && yarn start\" \"cd client-web && yarn start\"",
    "dev": "concurrently --kill-others-on-fail \"cd server && yarn dev\" \"cd client-web && yarn dev\""
  },
  "dependencies": {}
}
