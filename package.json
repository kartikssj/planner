{
  "name": "planner",
  "version": "1.0.0",
  "description": "Planner for Busy People",
  "scripts": {
    "install": "cd server && npm install && cd ../client-web && npm install",
    "build": "cd server && npm run build && cd ../client-web && npm run build",
    "start": "concurrently -k \"cd server && npm run start\" \"cd client-web && npm run start\""
  },
  "dependencies": {}
}
